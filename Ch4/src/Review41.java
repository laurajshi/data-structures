/**
 * Created by laurashi on 9/30/17.
 */
public class Review41
{
    public static void main (String [] args)
    {
        /*double sInitial= 4.0;
        double vInitial= 5.0;
        double t= 1.0;
        double g= 9.8;
        double s= (sInitial+ (vInitial*t)+(0.5*g*(Math.pow(t,2))));
        System.out.println(s);

        double m1= 6;
        double m2= 4;


        double PV= 5;
        double INT= 4;
        double YRS= 6;

        double FV= (PV * (Math.pow((1+(INT/100)), YRS)));
        double z= (Math.sqrt(Math.pow(a,2)+ Math.pow(b,2)-(2 * a* b * Math.cos(γ))));

        System.out.println(Math.atan(2)*(z, Math.sqrt( x * x +y*y));
        */

        double a= 1;
        double b= 1;
        double c= 1;



        double x1= ((-b - Math.sqrt(b*b -4 * a * c)) / 2*a);

        System.out.println(x1);


    }
}
